{
  "name": "url-shortener-frontend",
  "version": "1.0.0",
  "description": "Frontend build tools for URL Shortener",
  "scripts": {
    "sass": "sass src/main/webapp/styles/style.scss src/main/webapp/styles/style.css --style=expanded --source-map",
    "sass:watch": "sass --watch src/main/webapp/styles/style.scss src/main/webapp/styles/style.css --style=expanded --source-map"
  },
  "devDependencies": {
    "sass": "^1.89.1"
  }
}